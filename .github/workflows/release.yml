name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux (x86_64): muslで静的リンクビルドし、ファイル名はgnuとして出力
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            archive: tar.gz
            cross: true
            asset_target: x86_64-unknown-linux-gnu
            
          # Linux (ARM64): muslで静的リンクビルドし、ファイル名はgnuとして出力
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            archive: tar.gz
            cross: true
            asset_target: aarch64-unknown-linux-gnu
            
          # macOS (Intel)
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
            asset_target: x86_64-apple-darwin
            
          # macOS (Apple Silicon)
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
            asset_target: aarch64-apple-darwin
            
          # Windows
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip
            asset_target: x86_64-pc-windows-msvc

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross (for musl and ARM builds)
        if: matrix.cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build (cross)
        if: matrix.cross
        run: cross build --release --target ${{ matrix.target }}

      - name: Build (native)
        if: ${{ !matrix.cross }}
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (Unix)
        if: matrix.archive == 'tar.gz'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../dotfiles-installer-${{ matrix.asset_target }}.tar.gz dotfiles-installer
          cd ../../..

      - name: Package (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          Compress-Archive -Path "target/${{ matrix.target }}/release/dotfiles-installer.exe" -DestinationPath "dotfiles-installer-${{ matrix.asset_target }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dotfiles-installer-${{ matrix.asset_target }}
          path: dotfiles-installer-${{ matrix.asset_target }}.${{ matrix.archive }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/*
